/**
 * ThinkSpace — Shared Type Exports
 *
 * This file serves as the central export point for shared types
 * used across the application. Types specific to a feature should
 * remain in their respective feature directories.
 *
 * Types will be added as features are implemented in later phases.
 */

/** Common size variants used across UI components */
export type Size = "sm" | "md" | "lg" ;

/** Common component variant for visual distinction */
export type Variant = "primary" | "secondary" | "ghost" | "danger";
